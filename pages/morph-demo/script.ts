import KUTE from "kute.js";

const morph = KUTE.to("#Egg", { path: "#Pear" }, { duration: 1000 }).start();
// const showTip = KUTE.to("#Tip", { strokeDashoffset: 0 });

// morph.chain(showTip).start();
